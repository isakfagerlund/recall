{
  "name": "recall-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "deploy:api": "bun --filter api deploy"
  },
  "devDependencies": {
    "prettier": "3.7.4",
    "turbo": "^2.0.0",
    "typescript": "~5.9.2"
  },
  "packageManager": "bun@1.3.1",
  "engines": {
    "node": ">=18"
  }
}
